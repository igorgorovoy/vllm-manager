# vllm-tools direct dependencies
# Hardware: NVIDIA GB10 (aarch64) + CUDA 13.0 — vLLM wheel must be the cu130 build.
# Install: pip install -U -r requirements.txt

# vLLM engine + OpenAI-compatible API server (CLI + webapp)
vllm==0.19.0+cu130

# Hugging Face Hub client (weights download, `hf` CLI used by `vllm_models.py pull`)
huggingface_hub[cli]==1.11.0

# Webapp (FastAPI + SSE + subprocess/GPU introspection)
fastapi==0.135.3
uvicorn==0.44.0
sse-starlette==3.3.4
httpx==0.28.1
psutil==7.2.2
pydantic==2.13.0
